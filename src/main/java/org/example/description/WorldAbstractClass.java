package org.example.description;


/**
 * Данный класс релизовавывает симуляцию в которой происходят события
 * он принимает действующие лица и локацию, где происходят действия
 * между лицами. Класс реализует интерфейсы для проверки корректности
 * модели(на одинаковые объекты), также реализует интерфейсы для
 * задания локации: цвет неба, погоду, время(record-class)
 **/

public abstract class WorldAbstractClass {
    /*TODO*/
}
